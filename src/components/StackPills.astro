---
interface Props {
  items: readonly string[];
  tone?: 'light' | 'dark';
}

const { items, tone = 'light' } = Astro.props;
---

<ul class="flex flex-wrap gap-2">
  {items.map((item) => (
    <li
      class:list={[
        'rounded-full px-3.5 py-1.5 text-sm',
        tone === 'dark' ? 'bg-night-soft text-paper' : 'bg-sand text-ink',
      ]}
    >
      {item}
    </li>
  ))}
</ul>
